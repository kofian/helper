  <% if obj.errors.any? %>
    <div id="error_explanation">
      <div class="row">
       <div class="alert alert-danger col-md-8 col-md-offset-2">
      <h2><%= pluralize(obj.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
     </div>
     </div>
    </div>
  <% end %>
