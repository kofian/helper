<%= form_for(@donation) do |f| %>
  <% if @donation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@donation.errors.count, "error") %> prohibited this donation from being saved:</h2>

      <ul>
      <% @donation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="row">
 <div class="well col-md-8 col-md-offset-2 center">

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, value: current_user.name, size: 40 %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_area :address, rows: 3, cols: 40 %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, value: current_user.email,  size: 40 %>
  </div>
  <div class="field">
    <%= f.label :pay_type %><br>
    <%= f.select :pay_type, Donation::PAYMENT_TYPES, prompt: 'Select a payment method' %>
  </div>
  <div class="sesfield">
    <%= f.submit 'Donate' %>
  </div>
<% end %>

</div>
</div>

